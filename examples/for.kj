main : () -> int = {
    for i in [0..4]: print(i);

    L := [1..10, 2];
    for l in L: print(l);

    for i, l in L: L[i] = 2*l;
    for l in L: print(l);

    for ^l in L: l. += 1;
    for l in L: print(l);

    return 0;
}
