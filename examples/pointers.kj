main : () -> int = {
    p_int: ^int;

    x: int = 5;
    p_int = ^x;
    p_int. = 3;

    //p.alloc(int); // n: uint = 1 (default)
    //p .= 5;
    //p.free();
    //p = ^x;

    S : struct = {
        y: bool;
    }
    s: S;
    //p_S := Ptr.alloc(S);
    p_S := ^s;
    p_S.y = true; // autodereferencing

    return 0;
}
